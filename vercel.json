{
  "version": 2,
  "builds": [
    {
      "src": "scripts/build.js",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "Frontend/dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/Frontend/dist/$1"
    }
  ]
}
